 - Run `latexmk paper_randnoise.tex`.  What is the difference between the `.bib` and the `.bbl` file?
 - How does this bibliography style format a `misc` field?  (*hint*: look for `FUNCTION {misc}` in `siamplain.bst` ...yikes!)
